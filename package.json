{
  "name": "quizapp",
  "version": "1.0.0",
  "description": "Simple QuizApp",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "http-server -a localhost -p 9000 -c-1 ./app"
  },
  "author": "Khurram Saeed Malik",
  "license": "ISC",
  "devDependencies": {
    "bower": "^1.8.2",
    "http-server": "^0.10.0"
  }
}
